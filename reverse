#include <iostream>
using namespace std;

int main() {
    int i,rev=0,n;
    int rem;
    cout<<"enter number"<<endl;
    cin>>n;
    
    while(n!=0)
    {
        rem=n%10;
        rev=rev*10+rem;
        n=n/10;
    }
    cout<<"rev is"<<rev<<endl;
    return 0;
}
